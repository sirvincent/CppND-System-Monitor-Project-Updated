#ifndef FORMAT_H
#define FORMAT_H

#include <string>

namespace Format {
void formatNumber(std::string &number);
std::string ElapsedTime(long times);  // DONE: See src/format.cpp
};                                    // namespace Format

#endif
